{% extends 'base.html.twig' %}

{% block title %}AdvReservation{% endblock %}

{% block body %}
    <h1>AdvReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adv_reservation.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ adv_reservation.Message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adv_reservation_index') }}">back to list</a>

    <a href="{{ path('app_adv_reservation_edit', {'id': adv_reservation.id}) }}">edit</a>

    {{ include('adv_reservation/_delete_form.html.twig') }}
{% endblock %}
