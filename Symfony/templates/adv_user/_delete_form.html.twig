<form method="post" action="{{ path('app_adv_user_delete', {'id': adv_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adv_user.id) }}">
    <button class="btn">Delete</button>
</form>
