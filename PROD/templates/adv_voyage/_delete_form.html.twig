<form method="post" action="{{ path('app_adv_voyage_delete', {'id': adv_voyage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adv_voyage.id) }}">
    <button class="btn">Delete</button>
</form>
