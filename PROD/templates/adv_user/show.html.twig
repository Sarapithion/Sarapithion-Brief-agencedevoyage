{% extends 'base.html.twig' %}

{% block title %}AdvUser{% endblock %}

{% block body %}
    <h1>AdvUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adv_user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ adv_user.Nom }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ adv_user.Prénom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ adv_user.Mail }}</td>
            </tr>
            <tr>
                <th>Téléphone</th>
                <td>{{ adv_user.Téléphone }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ adv_user.Mdp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_adv_user_index') }}">back to list</a>

    <a href="{{ path('app_adv_user_edit', {'id': adv_user.id}) }}">edit</a>

    {{ include('adv_user/_delete_form.html.twig') }}
{% endblock %}
